name: mewati_tune_player
description: "Mewati Music Player - Mewati music ka ghar. A regional folk music streaming app."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.1
  supabase_flutter: ^2.5.0
  flutter_dotenv: ^5.1.0

  just_audio: ^0.9.46
  just_audio_background: ^0.0.1-beta.17

  background_downloader: ^9.5.9

  path_provider: ^2.1.2
  shared_preferences: ^2.2.2
  cached_network_image: ^3.3.1
  flutter_cache_manager: ^3.3.1
  intl: ^0.19.0
  http: ^1.2.0

  sentry_flutter: ^9.27.0
  connectivity_plus: ^5.0.2
  permission_handler: ^11.3.1
  speech_to_text: ^7.0.0
  wakelock_plus: ^1.2.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  fake_async: ^1.3.1
  path_provider_platform_interface: ^2.1.0

dependency_overrides:
  just_audio:
    path: packages/just_audio

flutter:
  uses-material-design: true
  assets:
    - assets/images/